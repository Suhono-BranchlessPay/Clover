# Milestone M3 — Display + Verify (planned)

**Status:** Planned (scaffold copied from Sunmi — history, WebView verify, share already in APK)  
**Repo:** https://github.com/Suhono-BranchlessPay/Clover/tree/dev

M2 APK already includes `TransactionHistoryActivity`, `VerifyActivity`, share verify URL, and Clover receipt stub. M3 sign-off = polish UI + Clover hardware screenshots.

See Sunmi M3 pattern for reference; Clover uses `CloverReceiptHelper` instead of Sunmi printer.
